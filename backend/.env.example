# Backend: MongoDB connection string
MONGO_URI=mongodb://localhost:27017/learn-central

# Backend: JWT secret used to sign tokens (use a long random string in production)
JWT_SECRET=your_jwt_secret_here

# Backend: port (optional)
PORT=4000

# Frontend: API base URL for dev
VITE_API_BASE=http://localhost:4000
